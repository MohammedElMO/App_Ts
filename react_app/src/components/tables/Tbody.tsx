

// @flow 
import * as React from 'react';

import {DataForm} from "../Form"

export const Tbody = (data:DataForm ) => {
    return (
        <tbody className=' border'>
            
        </tbody>            
    );
};